# Protected calls

Administrators can protect a call from retention on its detail page. Protection records the reason, identity, timestamp, and optional expiry without changing received metadata. Retention dry-runs and destructive runs exclude active protection; expired protection is eligible again. Protection changes require the existing admin authorization.
