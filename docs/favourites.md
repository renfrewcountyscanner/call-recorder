# Favourite groups

Favourite groups are global, system-scoped talkgroup collections managed from `/admin/favourites`. New groups are enabled by default and membership is unique by group, system, and talkgroup. The call list accepts `favourite=<group-id>` and preserves it through pagination. Per-user favourites are intentionally deferred until user roles exist.
